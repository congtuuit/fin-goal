import 'package:flutter/material.dart';
class MonthlyCheckinPage extends StatelessWidget {
  const MonthlyCheckinPage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Monthly Check-in — WIP')));
}
