import 'package:flutter/material.dart';
class ScenarioDashboardPage extends StatelessWidget {
  const ScenarioDashboardPage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Scenario Dashboard — WIP')));
}
