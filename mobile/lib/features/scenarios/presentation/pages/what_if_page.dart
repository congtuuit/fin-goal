import 'package:flutter/material.dart';
class WhatIfPage extends StatelessWidget {
  const WhatIfPage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('What-If — WIP')));
}
